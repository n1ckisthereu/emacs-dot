(defconst default-dir-notes "/home/nick/MEGA/Studing/Notes/")

(defconst com-pkg-org-libs
  '("requests"
    "colorama")
  "List of python packages to install in the Python ORG MODE environment.")

(provide 'com-variables)
